<RoundsScreen@MDScreen>:
    name: "Rounds"

    MDBoxLayout:
        orientation: "vertical"

        MDToolbar:
            title: "Rounds"
            left_action_items: [["menu", lambda x: app.root.get_screen("maininfo").ids.nav_drawer.set_state("open")]]
            right_action_items: [["close", lambda x: app.root.get_screen("maininfo").close()]]

        ScrollView:
            bar_width: "8dp"
            bar_color: C('FFFFFF')

            MDBoxLayout:
                orientation: "vertical"
                size_hint: None, None
                height: self.minimum_height
                size: (root.width, root.height)
                spacing: "8dp"
                padding: "8dp"

                MDLabel:
                    adaptive_height: True
                    text_color: C('FFFFFF')
                    theme_text_color: "Custom"
                    font_style: 'H6'
                    text: "Introduction"

                MDLabel:
                    adaptive_height: True
                    text_color: app.theme_cls.primary_color
                    theme_text_color: "Custom"
                    font_style: 'Body2'
                    text: "There are three rounds for each game. Depending on the round, depends on what kind and how many logs are provided. At any time you may review each round by using the tabs in the middle of the screen. Use this feature to see what logs are coming up next or to see what evidence was revealed previously."

                MDSeparator:
                    color: C("FFFFFF")

                MDLabel:
                    adaptive_height: True
                    text_color: C('FFFFFF')
                    theme_text_color: "Custom"
                    font_style: 'H6'
                    text: "Legend"

                MDLabel:
                    text_color: app.theme_cls.primary_color
                    font_style: "Body2"
                    text: f"[size=22sp][font=Icons]{md_icons['folder']}[/font][/size][color=#FFFFFF]: Unrevealed Evidence[/color]"
                    theme_text_color: "Custom"
                    adaptive_height: True
                    markup: True

                MDLabel:
                    text_color: C('FFFFFF')
                    font_style: "Body2"
                    text: f"[color=#1565c0][size=22sp][font=Icons]{md_icons['folder-key']}[/font][/size][/color]: Exclusive Evidence"
                    theme_text_color: "Custom"
                    adaptive_height: True
                    markup: True

                MDGridLayout:
                    adaptive_height: True
                    cols: 1
                    canvas.before:
                        Color:
                            rgba: 51/255, 255/255, 51/255, 0.3
                        Line:
                            width: 0.3
                            rectangle: self.x, self.y, self.width, self.height
                        Color:
                            rgba: 51/255, 255/255, 51/255, 0.06
                        Rectangle:
                            pos: self.pos
                            size: self.size

    # Round 1

                    MDLabel:
                        text_color: app.theme_cls.primary_color
                        font_style: "Body2"
                        text: f" [color=#FFFFFF][font=Icons][size=22sp]{md_icons['folder-search']}[/font][/size] Round 1:[/color] [size=22sp][font=Icons]{md_icons['folder']}[/font] [font=Icons]{md_icons['folder']}[/font] [font=Icons]{md_icons['folder']}[/font] [font=Icons]{md_icons['folder']}[/font][/size]"
                        theme_text_color: "Custom"
                        adaptive_height: True
                        markup: True

    # Round 2

                    MDLabel:
                        text_color: app.theme_cls.primary_color
                        font_style: "Body2"
                        md_bg_color: app.theme_cls.primary_dark
                        text: f" [color=#FFFFFF][font=Icons][size=22sp]{md_icons['folder-search']}[/font][/size] Round 2:[/color] [size=22sp][font=Icons]{md_icons['folder']}[/font] [font=Icons]{md_icons['folder']}[/font] [font=Icons]{md_icons['folder']}[/font][/size][color=#FFFFFF],[/color] [size=22sp][font=Icons]{md_icons['folder']}[/font] [font=Icons]{md_icons['folder']}[/font] [font=Icons]{md_icons['folder']}[/font][/size]"
                        theme_text_color: "Custom"
                        adaptive_height: True
                        markup: True

    # Round 3

                    MDLabel:
                        text_color: app.theme_cls.primary_color
                        font_style: "Body2"
                        text: f" [color=#FFFFFF][size=22sp][font=Icons]{md_icons['folder-search']}[/font][/size] Round 3:[/color][color=#1565c0] [size=22sp][font=Icons]{md_icons['folder-key']}[/font] [font=Icons]{md_icons['folder-key']}[/font][/size][/color][color=#FFFFFF],[/color] [color=#1565c0][size=22sp][font=Icons]{md_icons['folder-key']}[/font] [font=Icons]{md_icons['folder-key']}[/font][/size][/color]"
                        theme_text_color: "Custom"
                        adaptive_height: True
                        markup: True

                MDSeparator:
                    color: C("FFFFFF")

                MDLabel:
                    adaptive_height: True
                    text_color: C('FFFFFF')
                    theme_text_color: "Custom"
                    font_style: 'H6'
                    text: "Begin Round"

                MDLabel:
                    text_color: app.theme_cls.primary_color
                    font_style: "Body2"
                    text: f"In order to begin the next round, the [color=#FFFFFF][size=22sp][font=Icons]{md_icons['play']}[/font][/size][/color] at the bottom must be pressed. It is only available once all logs have been uploaded for the round and the timer has ended. The timer may also end prematurely if agreed upon. Please review the timer section for more information."
                    theme_text_color: "Custom"
                    adaptive_height: True
                    markup: True

                MDSeparator:
                    color: C("FFFFFF")

                MDLabel:
                    adaptive_height: True
                    text_color: C('FFFFFF')
                    theme_text_color: "Custom"
                    font_style: 'H6'
                    text: "Round Indicator"

                MDLabel:
                    text_color: app.theme_cls.primary_color
                    font_style: "Body2"
                    text: f"At the top of the screen there are three [color=#FFFFFF][size=22sp][font=Icons]{md_icons['circle-outline']}[/font][/size][/color]'s which represent each round. When a round begins, a [color=#FFFFFF][size=22sp][font=Icons]{md_icons['circle-outline']}[/font][/size][/color] becomes a [color=#FFFFFF][size=22sp][font=Icons]{md_icons['circle-slice-8']}[/font][/size][/color] to indicate what the current round is. The game ends once all three rounds have been completed. Once the game ends, a [color=#FFFFFF][size=22sp][font=Icons]{md_icons['chevron-right']}[/font][/size][/color] will appear at the bottom right of the screen which will take you to the results screen."
                    theme_text_color: "Custom"
                    adaptive_height: True
                    markup: True